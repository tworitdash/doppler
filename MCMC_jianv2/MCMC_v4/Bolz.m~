%% Testing the Gibbs/ Bolzmann Distribution

y = [1 2 3 2 1 3 4 5 7 8];
N = length(y);


data = y + 0.01 * randn(1, N);

c = 10;
C = linspace(1, c, c);

Ej = ;
beta = 1;

PrxC = exp(-beta * Ej)/sum(exp(-beta*Ej));

figure; plot()